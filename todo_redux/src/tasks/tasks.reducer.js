import { } from './tasks.actions'
 
const initState = {}

const tasksReducer = (state = initState, action) => {
  switch (action.type) {
    default: {
      return state;
    }
  }
};

export default tasksReducer;
